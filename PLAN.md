# Save Frontend Contrast And Sidebar Analytics Plan

## Summary
Create a Markdown planning document at `docs/frontend_contrast_sidebars_plan.md` containing the finalized implementation plan for improving hex color contrast and restoring sidebar analytics.

## Key Changes
- Add a new Markdown file with this title: `Restore Contrast And Sidebar Analytics`.
- Include sections for:
  - Summary
  - Key Changes
  - Test Plan
  - Assumptions
- Preserve the decisions from the current plan:
  - Percentile-clamped color scaling.
  - Persistent left analytics sidebar.
  - Right contextual hex/listing detail panel.
  - Real backend SHAP endpoints for global and hex-level explanations.
  - Keep current `PolygonLayer` rendering.

## Target File Content
Use the finalized plan text from the previous response, converted directly into normal Markdown without `<proposed_plan>` tags.

## Verification
- Confirm `docs/frontend_contrast_sidebars_plan.md` exists.
- Read the file back once to confirm the content was saved correctly.
